## 2026-08-26

### Vercel 构建修复
- **问题**: Vercel 部署报错 `Cannot find module 'lucide-react'`
- **根因**: 之前在移动项目到根目录时，`lucide-react` 从 `package.json` 中丢失了
- **修复**: 
  1. 将 `lucide-react: ^0.473.0` 添加回 `package.json` dependencies
  2. 清理并重新生成 `package-lock.json`
  3. 添加 `src/lucide.d.ts` 类型声明文件（lucide-react 和 recharts）
  4. 本地构建验证通过
  5. 提交并推送到 GitHub (`41a7669`)

### Header 双行布局
- 完成了之前未完成的 Header 两行布局重构
- 第一行：Logo + 公司名称（带 truncate）+ 语言切换器
- 第二行：4 个状态指示器完整显示

### EdgeOne 部署问题
- 多次尝试部署到 EdgeOne Makers 均失败
- 错误类型：npm install 超时 或 服务启动后 URL 不可达
- 可能是平台端临时问题，建议稍后重试或使用 Vercel

### 提交记录
- `3d5e8cf` - Header 双行布局 + lucide 类型声明
- `e05095d` - 修复：添加缺失的 lucide-react 依赖
- `74e2e22` - 修复：添加 recharts 类型声明 + 更新 lock 文件
- `41a7669` - 添加静态服务器脚本
